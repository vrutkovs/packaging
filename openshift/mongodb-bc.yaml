apiVersion: v1
kind: BuildConfig
metadata:
  creationTimestamp: null
  name: mongodb
spec:
  nodeSelector: null
  output:
    to:
      kind: ImageStreamTag
      name: mongodb:latest
  postCommit: {}
  resources: {}
  runPolicy: Serial
  source:
    contextDir: dockerfiles/centos/mongodb
    git:
      ref: docker
      uri: https://github.com/vrutkovs/packaging
    type: Git
  strategy:
    dockerStrategy:
      noCache: true
    type: Docker
  triggers:
    - type: configChange
status:
  lastVersion: 0
